<?php
header("Content-Type: application/json; charset=UTF-8");
$db["local"]["host"]="localhost:3308"; // host และ port ที่ใช้เชื่อม database
$db["local"]["username"]="dbuser"; // username ของ database
$db["local"]["password"]="password"; // password ของ database
$db["local"]["database"]="restaurant"; // ชื่อ database

